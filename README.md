# 🌍 Currency Service (Backend API)

[![Python](https://img.shields.io/badge/Python-3.12-blue?style=flat-square&logo=python)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-Framework-009688?style=flat-square&logo=fastapi)](https://fastapi.tiangolo.com/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?style=flat-square&logo=docker)](https://www.docker.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Managed-336791?style=flat-square&logo=postgresql)](https://www.postgresql.org/)

**Currency Service** — это современный асинхронный бэкенд-сервис для автоматизированного сбора, обработки и предоставления данных о курсах валют. Проект спроектирован как отказоустойчивый микросервис, готовый к высоким нагрузкам и легкой масштабируемости.

---

## 🏗 Архитектура системы

Сервис реализует паттерн «Адаптер» для работы с внешними финансовыми шлюзами, обеспечивая единообразный интерфейс данных внутри системы.



### Ключевые технические решения:
* **Asynchronous Core:** Построен на базе `FastAPI`, что позволяет обрабатывать тысячи одновременных соединений при минимальном потреблении ресурсов.
* **Distributed Caching:** Интеграция с `Redis` для кэширования ответов внешних API. Это гарантирует мгновенный отклик (Low Latency) и защищает от лимитов (Rate Limits) сторонних провайдеров.
* **Data Integrity:** Использование `Pydantic` для строгой валидации входящих и исходящих данных (DTO).
* **Background Tasks:** Поддержка асинхронных задач для периодического обновления курсов в фоновом режиме.

---

## 🛠 Технологический стек

* **Language:** Python 3.12+
* **Web Framework:** FastAPI (Starlette)
* **HTTP Client:** `aiohttp` (Asynchronous requests)
* **Environment:** `python-dotenv`
* **Server:** `Uvicorn` / `Gunicorn`
* **Deployment:** Docker, Docker Compose

---

## 🚀 Быстрый запуск (Deployment)

### Подготовка окружения
Склонируйте репозиторий и настройте переменные среды:

```bash
git clone git@github.com:Force-in-Motion/Currency-service.git
cd Currency-service
cp .env   .env.docker
```

### Запуск инфраструктуры (Docker)
Проект полностью контейнеризирован. Для запуска всех компонентов (API + Redis + DB) выполните:

```bash
docker compose --env-file .env.docker up -d --build
```

### Проверка работоспособности
    После запуска API будет доступно по адресу: http://localhost:8000


### 🔌 API Documentation

Сервис автоматически генерирует интерактивную документацию по стандарту OpenAPI.

Swagger UI: http://localhost:8000/docs — для ручного тестирования.

Redoc: http://localhost:8000/redoc — расширенная документация.


### Основные эндпоинты:

Метод,Путь,Описание

GET,/api/v1/rates,Получить список всех актуальных курсов валют
GET,/api/v1/rates/{code},Получить курс конкретной валюты по её коду
GET,/api/v1/convert,Конвертация суммы из одной валюты в другую
GET,/health,Liveness/Readiness probe для мониторинга

### 📂 Структура проекта
```bash
Currency-service/
├── src/                 # Исходный код приложения
│   ├── api/             # Эндпоинты и контроллеры
│   ├── core/            # Конфигурация, логирование, настройки
│   ├── services/        # Бизнес-логика и интеграция с внешними API
│   ├── schemas/         # Pydantic модели (валидация)
│   └── main.py          # Точка входа (FastAPI app)
├── .env.docker          # Конфигурация контейнеров
├── Dockerfile           # Инструкция сборки образа
└── docker-compose.yml   # Оркестрация сервисов
```

### 👤 Автор
Force-in-Motion * GitHub: @Force-in-Motion

    Project: Currency-service (Backend)